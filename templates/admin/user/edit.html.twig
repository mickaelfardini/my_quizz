{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
<div class="container">
	<div class="title text-center justify-content-center">
		<h1>Edit User</h1>
	</div>

	{{ include('admin/user/_form.html.twig', {'button_label': 'Update'}) }}

	<a class="mt-4" href="{{ path('user_index') }}">back to list</a>

	{{ include('admin/user/_delete_form.html.twig') }}
</div>
{% endblock %}
